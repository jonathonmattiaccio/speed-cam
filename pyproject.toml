[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "speed-cam"
version = "0.1.0"
description = "Starter speed camera architecture for Raspberry Pi"
requires-python = ">=3.11"

[tool.pytest.ini_options]
testpaths = ["tests"]
